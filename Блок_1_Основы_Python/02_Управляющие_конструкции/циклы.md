# 🔁 Циклы for и while

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)

---

## 🎯 Цель изучения

Освоить работу с циклами для повторения операций и обработки последовательностей данных.

---

## 📚 Теория

### Цикл for

Используется для итерации по последовательности (список, строка, диапазон и т.д.).

```python
# Итерация по списку
fruits = ["яблоко", "банан", "апельсин"]
for fruit in fruits:
    print(fruit)

# Итерация по строке
for char in "Python":
    print(char)

# Итерация по диапазону
for i in range(5):
    print(i)  # 0, 1, 2, 3, 4
```

### Функция range()

```python
# range(stop)
for i in range(5):
    print(i)  # 0, 1, 2, 3, 4

# range(start, stop)
for i in range(2, 6):
    print(i)  # 2, 3, 4, 5

# range(start, stop, step)
for i in range(0, 10, 2):
    print(i)  # 0, 2, 4, 6, 8

# Обратный порядок
for i in range(10, 0, -1):
    print(i)  # 10, 9, 8, ..., 1
```

### Цикл while

Выполняется пока условие истинно.

```python
counter = 0
while counter < 5:
    print(counter)
    counter += 1

# Бесконечный цикл (с выходом)
while True:
    user_input = input("Введите 'выход' для остановки: ")
    if user_input == "выход":
        break
    print(f"Вы ввели: {user_input}")
```

### Enumerate и zip

```python
# enumerate - получение индекса и значения
fruits = ["яблоко", "банан", "апельсин"]
for index, fruit in enumerate(fruits):
    print(f"{index}: {fruit}")

# Начать с другого числа
for index, fruit in enumerate(fruits, start=1):
    print(f"{index}. {fruit}")

# zip - итерация по нескольким последовательностям
names = ["Алиса", "Боб", "Чарли"]
ages = [25, 30, 35]
for name, age in zip(names, ages):
    print(f"{name}: {age} лет")
```

---

## 💻 Практические примеры

### Пример 1: Таблица умножения

```python
number = int(input("Введите число для таблицы умножения: "))

print(f"\n=== Таблица умножения на {number} ===")
for i in range(1, 11):
    result = number * i
    print(f"{number} × {i:2d} = {result:3d}")
```

### Пример 2: Сумма чисел

```python
# Использование for
total = 0
for i in range(1, 101):
    total += i
print(f"Сумма чисел от 1 до 100: {total}")

# Использование while
total = 0
number = 1
while number <= 100:
    total += number
    number += 1
print(f"Сумма чисел от 1 до 100: {total}")
```

### Пример 3: Проверка пароля

```python
CORRECT_PASSWORD = "python123"
MAX_ATTEMPTS = 3

attempts = 0
while attempts < MAX_ATTEMPTS:
    password = input(f"Введите пароль (попытка {attempts + 1}/{MAX_ATTEMPTS}): ")
    
    if password == CORRECT_PASSWORD:
        print("✅ Вход выполнен успешно!")
        break
    else:
        attempts += 1
        remaining = MAX_ATTEMPTS - attempts
        if remaining > 0:
            print(f"❌ Неверный пароль. Осталось попыток: {remaining}")
else:
    print("🔒 Доступ заблокирован!")
```

### Пример 4: Рисование фигур

```python
# Квадрат
size = int(input("Размер квадрата: "))
for i in range(size):
    print("* " * size)

print()

# Прямоугольный треугольник
height = int(input("Высота треугольника: "))
for i in range(1, height + 1):
    print("* " * i)

print()

# Пирамида
height = int(input("Высота пирамиды: "))
for i in range(1, height + 1):
    spaces = " " * (height - i)
    stars = "* " * i
    print(spaces + stars)
```

### Пример 5: Меню программы

```python
menu = """
╔════════════════════════╗
║       ГЛАВНОЕ МЕНЮ     ║
╠════════════════════════╣
║ 1. Добавить задачу     ║
║ 2. Показать задачи     ║
║ 3. Удалить задачу      ║
║ 4. Выход               ║
╚════════════════════════╝
"""

tasks = []

while True:
    print(menu)
    choice = input("Выберите действие: ")
    
    if choice == "1":
        task = input("Введите задачу: ")
        tasks.append(task)
        print("✅ Задача добавлена!")
    
    elif choice == "2":
        if tasks:
            print("\n📝 Ваши задачи:")
            for i, task in enumerate(tasks, 1):
                print(f"{i}. {task}")
        else:
            print("📭 Задач нет")
    
    elif choice == "3":
        if tasks:
            print("\n📝 Текущие задачи:")
            for i, task in enumerate(tasks, 1):
                print(f"{i}. {task}")
            index = int(input("Номер задачи для удаления: ")) - 1
            if 0 <= index < len(tasks):
                removed = tasks.pop(index)
                print(f"🗑️ Удалено: {removed}")
            else:
                print("❌ Неверный номер")
        else:
            print("📭 Задач нет")
    
    elif choice == "4":
        print("👋 До свидания!")
        break
    
    else:
        print("❌ Неверный выбор!")
    
    input("\nНажмите Enter для продолжения...")
```

### Пример 6: Анализ текста

```python
text = input("Введите текст: ")

vowels = "аеёиоуыэюяaeiouy"
consonants = "бвгджзйклмнпрстфхцчшщbcdfghjklmnpqrstvwxyz"

vowel_count = 0
consonant_count = 0
digit_count = 0
space_count = 0

for char in text.lower():
    if char in vowels:
        vowel_count += 1
    elif char in consonants:
        consonant_count += 1
    elif char.isdigit():
        digit_count += 1
    elif char.isspace():
        space_count += 1

print(f"\n=== АНАЛИЗ ТЕКСТА ===")
print(f"Всего символов: {len(text)}")
print(f"Гласных: {vowel_count}")
print(f"Согласных: {consonant_count}")
print(f"Цифр: {digit_count}")
print(f"Пробелов: {space_count}")
```

---

## ✅ Задачи для практики

### Задача 1: Факториал
Напишите программу, которая вычисляет факториал числа (n! = 1 × 2 × 3 × ... × n).

<details>
<summary>💡 Решение</summary>

```python
n = int(input("Введите число: "))
factorial = 1

for i in range(1, n + 1):
    factorial *= i

print(f"{n}! = {factorial}")
```
</details>

### Задача 2: Числа Фибоначчи
Выведите первые N чисел последовательности Фибоначчи (каждое следующее число = сумма двух предыдущих: 0, 1, 1, 2, 3, 5, 8...).

### Задача 3: Проверка простого числа
Создайте программу, которая проверяет, является ли число простым.

### Задача 4: Калькулятор среднего
Программа запрашивает оценки, пока пользователь не введёт "стоп", затем выводит среднее арифметическое.

### Задача 5: Угадай число
Компьютер загадывает число от 1 до 100. Пользователь угадывает, программа подсказывает "больше"/"меньше". Подсчитайте количество попыток.

### Задача 6: Переворот числа
Дано число, выведите его в обратном порядке. Например: 12345 → 54321

---

## 🎓 Продвинутые техники

### 1. List Comprehension

```python
# Создание списка в одну строку
squares = [x**2 for x in range(10)]
print(squares)  # [0, 1, 4, 9, 16, 25, 36, 49, 64, 81]

# С условием
even_squares = [x**2 for x in range(10) if x % 2 == 0]
print(even_squares)  # [0, 4, 16, 36, 64]

# Вложенный
matrix = [[i+j for j in range(3)] for i in range(3)]
print(matrix)  # [[0, 1, 2], [1, 2, 3], [2, 3, 4]]
```

### 2. Dict Comprehension

```python
# Создание словаря
squares_dict = {x: x**2 for x in range(5)}
print(squares_dict)  # {0: 0, 1: 1, 2: 4, 3: 9, 4: 16}

# Инверсия словаря
original = {"a": 1, "b": 2, "c": 3}
inverted = {v: k for k, v in original.items()}
print(inverted)  # {1: 'a', 2: 'b', 3: 'c'}
```

### 3. Вложенные циклы

```python
# Таблица умножения
for i in range(1, 6):
    for j in range(1, 6):
        print(f"{i*j:3}", end=" ")
    print()

# Перебор координат
for x in range(3):
    for y in range(3):
        print(f"({x}, {y})", end=" ")
    print()
```

### 4. Else в циклах

```python
# else выполняется, если цикл НЕ прерван break
for i in range(5):
    if i == 10:  # Никогда не True
        break
else:
    print("Цикл завершён нормально")  # Выполнится

# Поиск числа
numbers = [1, 2, 3, 4, 5]
target = 6

for num in numbers:
    if num == target:
        print("Найдено!")
        break
else:
    print("Не найдено!")  # Выполнится
```

---

## 💡 Лучшие практики

### ✅ Рекомендуется

```python
# 1. Используйте for для известного количества итераций
for i in range(10):  # ✅
    print(i)

# 2. Используйте while для условных циклов
while user_wants_continue():  # ✅
    do_something()

# 3. Избегайте изменения списка во время итерации
# ❌ Плохо
for item in items:
    if condition:
        items.remove(item)

# ✅ Хорошо
items = [item for item in items if not condition]

# 4. Используйте enumerate вместо range(len())
# ❌ Плохо
for i in range(len(items)):
    print(i, items[i])

# ✅ Хорошо
for i, item in enumerate(items):
    print(i, item)
```

---

## 🔗 Связанные темы

- [Условные операторы](условные_операторы.md)
- [Операторы break, continue, pass](операторы_цикла.md)
- [Списки](../03_Структуры_данных/списки.md)
- [Итераторы и генераторы](../../Блок_3_Стандартная_библиотека/02_Итераторы_генераторы/итераторы.md)

---

## 📖 Ресурсы для изучения

- [Python Docs - For Loops](https://docs.python.org/3/tutorial/controlflow.html#for-statements)
- [Real Python - Python "for" Loops](https://realpython.com/python-for-loop/)
- [Real Python - Python "while" Loops](https://realpython.com/python-while-loop/)

---

**Следующая тема:** [Операторы break, continue, pass →](операторы_цикла.md)

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)
