# 🔀 Условные операторы

[← Вернуться к roadmap](README.md)

---

## 🎯 Цель изучения

Научиться использовать условные конструкции для принятия решений в программе на основе различных условий.

---

## 📚 Теория

### Оператор if

Базовая условная конструкция:

```python
if условие:
    # код выполняется если условие истинно
    pass
```

### Конструкция if-else

```python
age = 18

if age >= 18:
    print("Вы совершеннолетний")
else:
    print("Вы несовершеннолетний")
```

### Конструкция if-elif-else

```python
score = 85

if score >= 90:
    grade = "Отлично"
elif score >= 75:
    grade = "Хорошо"
elif score >= 60:
    grade = "Удовлетворительно"
else:
    grade = "Неудовлетворительно"

print(f"Оценка: {grade}")
```

### Вложенные условия

```python
age = 25
has_license = True

if age >= 18:
    if has_license:
        print("Можете водить автомобиль")
    else:
        print("Нужны права")
else:
    print("Слишком молоды для вождения")
```

### Тернарный оператор

```python
# Краткая форма if-else
age = 20
status = "Взрослый" if age >= 18 else "Ребёнок"

# Эквивалентно:
if age >= 18:
    status = "Взрослый"
else:
    status = "Ребёнок"
```

---

## 💻 Практические примеры

### Пример 1: Калькулятор скидок

```python
print("=== КАЛЬКУЛЯТОР СКИДОК ===\n")

total = float(input("Введите сумму покупки: "))

if total >= 10000:
    discount = 0.20
    message = "Скидка 20%!"
elif total >= 5000:
    discount = 0.15
    message = "Скидка 15%!"
elif total >= 2000:
    discount = 0.10
    message = "Скидка 10%!"
else:
    discount = 0
    message = "Скидка не применяется"

final_price = total * (1 - discount)

print(f"\n{message}")
print(f"Сумма без скидки: {total:.2f} руб.")
print(f"Скидка: {total * discount:.2f} руб.")
print(f"К оплате: {final_price:.2f} руб.")
```

### Пример 2: Проверка високосного года

```python
year = int(input("Введите год: "))

if year % 400 == 0:
    is_leap = True
elif year % 100 == 0:
    is_leap = False
elif year % 4 == 0:
    is_leap = True
else:
    is_leap = False

if is_leap:
    print(f"{year} год - високосный (366 дней)")
else:
    print(f"{year} год - не високосный (365 дней)")
```

### Пример 3: Категоризация возраста

```python
age = int(input("Введите возраст: "))

if age < 0:
    print("❌ Некорректный возраст!")
elif age <= 2:
    category = "Младенец"
    emoji = "👶"
elif age <= 12:
    category = "Ребёнок"
    emoji = "🧒"
elif age <= 19:
    category = "Подросток"
    emoji = "🧑"
elif age <= 35:
    category = "Молодой"
    emoji = "👨"
elif age <= 60:
    category = "Взрослый"
    emoji = "👴"
else:
    category = "Пожилой"
    emoji = "👴"

print(f"{emoji} Вы относитесь к категории: {category}")
```

### Пример 4: Вход в систему

```python
CORRECT_USERNAME = "admin"
CORRECT_PASSWORD = "12345"

print("=== ВХОД В СИСТЕМУ ===\n")

username = input("Логин: ")
password = input("Пароль: ")

if username == CORRECT_USERNAME and password == CORRECT_PASSWORD:
    print("\n✅ Вход выполнен успешно!")
    print("Добро пожаловать в систему!")
elif username == CORRECT_USERNAME:
    print("\n❌ Неверный пароль!")
elif password == CORRECT_PASSWORD:
    print("\n❌ Неверный логин!")
else:
    print("\n❌ Неверный логин и пароль!")
```

### Пример 5: Определение времени суток

```python
hour = int(input("Введите час (0-23): "))

if 0 <= hour < 6:
    time_of_day = "Ночь"
    greeting = "Доброй ночи"
    emoji = "🌙"
elif 6 <= hour < 12:
    time_of_day = "Утро"
    greeting = "Доброе утро"
    emoji = "🌅"
elif 12 <= hour < 18:
    time_of_day = "День"
    greeting = "Добрый день"
    emoji = "☀️"
elif 18 <= hour < 24:
    time_of_day = "Вечер"
    greeting = "Добрый вечер"
    emoji = "🌆"
else:
    time_of_day = "Некорректное время"
    greeting = "Ошибка"
    emoji = "❌"

print(f"\n{emoji} {greeting}! Сейчас {time_of_day}.")
```

---

## ✅ Задачи для практики

### Задача 1: Калькулятор ИМТ
Создайте программу, которая:
1. Запрашивает вес (кг) и рост (м)
2. Вычисляет ИМТ = вес / рост²
3. Определяет категорию:
   - < 18.5: Недостаточный вес
   - 18.5-24.9: Нормальный вес
   - 25-29.9: Избыточный вес
   - ≥ 30: Ожирение

### Задача 2: Проверка треугольника
Даны три стороны треугольника. Проверьте:
1. Можно ли построить треугольник с такими сторонами?
2. Если да, то какой он: равносторонний, равнобедренный или разносторонний?

<details>
<summary>💡 Подсказка</summary>

Треугольник существует, если сумма любых двух сторон больше третьей:
- a + b > c
- a + c > b
- b + c > a
</details>

### Задача 3: Определение сезона
По номеру месяца (1-12) определите сезон года и выведите соответствующую информацию.

### Задача 4: Конвертер оценок
Создайте программу, которая конвертирует оценки:
- Российская система (2-5) ↔ Международная (F-A)
- 5 → A, 4 → B, 3 → C, 2 → F

### Задача 5: Игра "Угадай число"
Загадайте число от 1 до 100. Дайте пользователю 3 попытки угадать. После каждой попытки подсказывайте: "больше" или "меньше".

---

## 🎓 Продвинутые техники

### 1. Множественные условия

```python
# Использование and
age = 25
has_license = True
if age >= 18 and has_license:
    print("Можете водить")

# Использование or
day = "Saturday"
if day == "Saturday" or day == "Sunday":
    print("Выходной!")

# Использование not
is_weekend = False
if not is_weekend:
    print("Рабочий день")
```

### 2. Проверка на вхождение

```python
# Проверка в списке
allowed_users = ["admin", "user1", "user2"]
username = input("Введите логин: ")

if username in allowed_users:
    print("Доступ разрешён")
else:
    print("Доступ запрещён")

# Проверка в строке
email = input("Введите email: ")
if "@" in email and "." in email:
    print("Email выглядит корректно")
```

### 3. Короткое вычисление (Short-circuit evaluation)

```python
# and: если первое False, второе не проверяется
x = 0
if x != 0 and 10 / x > 1:  # Безопасно: деление не выполнится
    print("Условие истинно")

# or: если первое True, второе не проверяется
name = input("Имя: ")
display_name = name or "Аноним"  # Если name пустое, используется "Аноним"
```

### 4. Match-case (Python 3.10+)

```python
# Современная альтернатива множественным if-elif
command = input("Введите команду: ")

match command:
    case "start":
        print("Запуск программы...")
    case "stop":
        print("Остановка программы...")
    case "restart":
        print("Перезапуск программы...")
    case _:
        print("Неизвестная команда")
```

### 5. Проверка нескольких значений

```python
# Вместо множества or
status = input("Статус: ")
if status in ["active", "pending", "processing"]:
    print("Заказ в обработке")

# С match-case
match status:
    case "active" | "pending" | "processing":
        print("Заказ в обработке")
    case "completed":
        print("Заказ завершён")
    case _:
        print("Неизвестный статус")
```

---

## 💡 Лучшие практики

### ✅ Рекомендуется

```python
# 1. Явные сравнения с None, True, False
if value is None:  # ✅
    pass

if flag is True:  # ✅ (но можно просто if flag:)
    pass

# 2. Использование in для множественных проверок
if x in [1, 2, 3, 4, 5]:  # ✅
    pass

# 3. Инверсия условий для раннего выхода
def process_user(user):
    if not user:  # ✅ Ранний выход
        return
    
    # Основная логика
    pass
```

### ❌ Не рекомендуется

```python
# 1. Сравнение с True/False через ==
if flag == True:  # ❌
    pass
# Лучше:
if flag:  # ✅
    pass

# 2. Множественные or
if x == 1 or x == 2 or x == 3:  # ❌
    pass
# Лучше:
if x in [1, 2, 3]:  # ✅
    pass

# 3. Глубокая вложенность
if condition1:  # ❌
    if condition2:
        if condition3:
            if condition4:
                pass
# Лучше: ранний выход или объединение условий
```

---

## 🔗 Связанные темы

- [Операторы](../01_Синтаксис_и_базовые_концепции/операторы.md)
- [Циклы](циклы.md)
- [Операторы break, continue, pass](операторы_цикла.md)

---

## 📖 Ресурсы для изучения

- [Python Docs - Control Flow](https://docs.python.org/3/tutorial/controlflow.html)
- [Real Python - Conditional Statements](https://realpython.com/python-conditional-statements/)
- [PEP 634 - Structural Pattern Matching](https://www.python.org/dev/peps/pep-0634/)

---

**Следующая тема:** [Циклы →](циклы.md)

[← Вернуться к roadmap](README.md)
