# ⏯️ Операторы break, continue, pass

[← Вернуться к roadmap](README.md)

---

## 🎯 Цель изучения

Научиться управлять выполнением циклов с помощью операторов break, continue и pass.

---

## 📚 Теория

### Оператор break

Прерывает выполнение цикла полностью.

```python
for i in range(10):
    if i == 5:
        break  # Выход из цикла
    print(i)
# Вывод: 0 1 2 3 4
```

### Оператор continue

Пропускает текущую итерацию и переходит к следующей.

```python
for i in range(10):
    if i % 2 == 0:
        continue  # Пропуск чётных чисел
    print(i)
# Вывод: 1 3 5 7 9
```

### Оператор pass

Ничего не делает (заглушка).

```python
for i in range(5):
    if i == 3:
        pass  # Ничего не происходит
    print(i)
# Вывод: 0 1 2 3 4

# Используется как заглушка
def future_function():
    pass  # TODO: реализовать позже

class FutureClass:
    pass  # Пустой класс
```

---

## 💻 Практические примеры

### Пример 1: Поиск в списке

```python
numbers = [10, 23, 45, 78, 32, 90, 15]
target = 32

for index, num in enumerate(numbers):
    if num == target:
        print(f"✅ Число {target} найдено на позиции {index}")
        break
else:
    print(f"❌ Число {target} не найдено")
```

### Пример 2: Пропуск негативных чисел

```python
numbers = [10, -5, 23, -8, 42, -3, 15]

print("Положительные числа:")
for num in numbers:
    if num < 0:
        continue
    print(num)
```

### Пример 3: Валидация ввода

```python
MAX_ATTEMPTS = 3
attempts = 0

while attempts < MAX_ATTEMPTS:
    age = input("Введите возраст (18-100): ")
    
    if not age.isdigit():
        print("❌ Введите число!")
        attempts += 1
        continue
    
    age = int(age)
    
    if age < 18 or age > 100:
        print("❌ Возраст должен быть от 18 до 100")
        attempts += 1
        continue
    
    print(f"✅ Возраст {age} принят")
    break
else:
    print("🔒 Превышено количество попыток")
```

### Пример 4: Фильтрация данных

```python
students = [
    {"name": "Алиса", "grade": 85},
    {"name": "Боб", "grade": 45},
    {"name": "Чарли", "grade": 92},
    {"name": "Дэвид", "grade": 38},
    {"name": "Ева", "grade": 78},
]

print("Студенты, сдавшие экзамен (>= 60):")
for student in students:
    if student["grade"] < 60:
        continue  # Пропускаем не сдавших
    print(f"- {student['name']}: {student['grade']} баллов")
```

---

## 📖 Ресурсы для изучения

- [Python Docs - Break and Continue](https://docs.python.org/3/tutorial/controlflow.html#break-and-continue-statements-and-else-clauses-on-loops)

---

**Следующая тема:** [Списки →](../03_Структуры_данных/списки.md)

[← Вернуться к roadmap](README.md)
