# 📝 Переменные и типы данных

[← Вернуться к roadmap](README.md)

---

## 🎯 Цель изучения

Научиться работать с основными типами данных в Python: целыми числами, числами с плавающей точкой, строками и логическими значениями.

---

## 📚 Теория

### Что такое переменная?

Переменная — это именованная область памяти, в которой хранится значение. В Python не нужно явно объявлять тип переменной — он определяется автоматически.

```python
# Создание переменной
name = "Alice"
age = 25
```

### Основные типы данных

#### 1. **int (целые числа)**

```python
age = 25
year = 2025
negative = -10
big_number = 1_000_000  # Можно использовать _ для удобства чтения
```

#### 2. **float (числа с плавающей точкой)**

```python
price = 99.99
temperature = -3.5
pi = 3.14159
scientific = 1.5e-4  # Научная нотация: 0.00015
```

#### 3. **str (строки)**

```python
name = "Alice"
message = 'Hello, World!'
multiline = """Это
многострочная
строка"""
```

#### 4. **bool (логические значения)**

```python
is_active = True
is_deleted = False
```

### Проверка типа данных

```python
x = 42
print(type(x))  # <class 'int'>

y = 3.14
print(type(y))  # <class 'float'>

z = "Hello"
print(type(z))  # <class 'str'>
```

### Преобразование типов

```python
# В строку
num = 42
text = str(num)  # "42"

# В число
text = "100"
num = int(text)  # 100
float_num = float(text)  # 100.0

# В логический тип
bool(0)  # False
bool(1)  # True
bool("")  # False
bool("text")  # True
```

---

## 💻 Практические примеры

### Пример 1: Работа с разными типами данных

```python
# Переменные
name = "Иван"
age = 28
height = 1.75
is_student = False

# Вывод информации
print(f"Имя: {name}")
print(f"Возраст: {age} лет")
print(f"Рост: {height} м")
print(f"Студент: {is_student}")

# Проверка типов
print(f"Тип name: {type(name)}")
print(f"Тип age: {type(age)}")
print(f"Тип height: {type(height)}")
print(f"Тип is_student: {type(is_student)}")
```

### Пример 2: Преобразование типов

```python
# Пользовательский ввод всегда возвращает строку
age_input = input("Введите ваш возраст: ")
print(f"Тип введённого значения: {type(age_input)}")

# Преобразование в число
age = int(age_input)
next_year_age = age + 1
print(f"В следующем году вам будет: {next_year_age}")
```

### Пример 3: Калькулятор с типами данных

```python
# Ввод данных
price = float(input("Введите цену товара: "))
quantity = int(input("Введите количество: "))
discount = float(input("Введите скидку в % (например, 10): "))

# Расчёты
subtotal = price * quantity
discount_amount = subtotal * (discount / 100)
total = subtotal - discount_amount

# Вывод
print(f"\nПромежуточная сумма: {subtotal:.2f} руб.")
print(f"Скидка: {discount_amount:.2f} руб.")
print(f"Итого к оплате: {total:.2f} руб.")
```

---

## ✅ Задачи для практики

### Задача 1: Обмен значениями
Создайте две переменные `a = 5` и `b = 10`. Поменяйте их значения местами и выведите результат.

<details>
<summary>💡 Подсказка</summary>

```python
a = 5
b = 10
# Способ 1: с временной переменной
temp = a
a = b
b = temp

# Способ 2: Python way
a, b = b, a
```
</details>

### Задача 2: Информация о человеке
Создайте программу, которая запрашивает у пользователя:
- Имя (строка)
- Возраст (целое число)
- Рост в метрах (число с плавающей точкой)
- Является ли студентом (True/False)

Выведите всю информацию в красивом формате.

### Задача 3: Конвертер температуры
Напишите программу, которая:
1. Запрашивает температуру в Цельсиях
2. Переводит её в Фаренгейты по формуле: F = C × 9/5 + 32
3. Выводит результат с точностью до 2 знаков после запятой

### Задача 4: Проверка типов
Создайте переменные разных типов и выведите для каждой:
- Значение
- Тип
- Результат преобразования в строку

---

## 🎓 Дополнительные материалы

### Правила именования переменных

✅ **Правильно:**
```python
user_name = "Alice"
age_2025 = 25
_private = "secret"
CONSTANT_VALUE = 100
```

❌ **Неправильно:**
```python
2_age = 25  # Не может начинаться с цифры
user-name = "Alice"  # Дефис запрещён
class = "Python"  # Зарезервированное слово
```

### Полезные функции

```python
# isinstance() - проверка типа
x = 42
print(isinstance(x, int))  # True
print(isinstance(x, str))  # False

# id() - адрес в памяти
a = 100
print(id(a))

# sys.getsizeof() - размер в байтах
import sys
print(sys.getsizeof(42))
```

---

## 🔗 Связанные темы

- [Операторы](операторы.md)
- [Ввод и вывод данных](ввод_вывод.md)
- [Строки и методы обработки](../03_Структуры_данных/строки.md)

---

## 📖 Ресурсы для изучения

- [Python Docs - Built-in Types](https://docs.python.org/3/library/stdtypes.html)
- [Real Python - Basic Data Types](https://realpython.com/python-data-types/)
- [W3Schools - Python Variables](https://www.w3schools.com/python/python_variables.asp)

---

**Следующая тема:** [Операторы →](операторы.md)

[← Вернуться к roadmap](README.md)
