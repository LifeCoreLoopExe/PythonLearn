# 📝 Комментарии и документация

[← Вернуться к roadmap](README.md)

---

## 🎯 Цель изучения

Научиться правильно комментировать код и создавать документацию для функций, классов и модулей.

---

## 📚 Теория

### Виды комментариев

#### 1. Однострочные комментарии

```python
# Это однострочный комментарий
x = 5  # Комментарий может быть и в конце строки

# Присваиваем начальное значение
counter = 0
```

#### 2. Многострочные комментарии

```python
"""
Это многострочный комментарий.
Он может занимать несколько строк.
Обычно используется для документации.
"""

'''
Также можно использовать одинарные кавычки.
Но двойные кавычки более приняты в сообществе.
'''
```

#### 3. Docstrings (документация)

```python
def calculate_area(length, width):
    """
    Вычисляет площадь прямоугольника.
    
    Args:
        length (float): Длина прямоугольника
        width (float): Ширина прямоугольника
    
    Returns:
        float: Площадь прямоугольника
    
    Examples:
        >>> calculate_area(5, 3)
        15.0
    """
    return length * width
```

---

## 💻 Практические примеры

### Пример 1: Правильное использование комментариев

```python
# ❌ ПЛОХО - очевидные комментарии
x = 5  # присваиваем x значение 5
y = x + 10  # прибавляем к x число 10

# ✅ ХОРОШО - объясняем "почему", а не "что"
TAX_RATE = 0.20  # НДС в России составляет 20%
price_with_tax = price * (1 + TAX_RATE)

# ✅ ХОРОШО - объясняем сложную логику
# Проверяем високосный год по правилам григорианского календаря
is_leap = (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)
```

### Пример 2: Документация функции

```python
def validate_email(email):
    """
    Проверяет корректность email адреса.
    
    Функция выполняет базовую валидацию email:
    - Наличие символа @
    - Наличие точки после @
    - Минимальная длина
    
    Args:
        email (str): Email адрес для проверки
    
    Returns:
        bool: True если email валидный, иначе False
    
    Raises:
        TypeError: Если email не является строкой
    
    Examples:
        >>> validate_email("user@example.com")
        True
        >>> validate_email("invalid.email")
        False
    
    Note:
        Это упрощённая проверка, для production используйте
        библиотеку email-validator
    """
    if not isinstance(email, str):
        raise TypeError("Email должен быть строкой")
    
    if len(email) < 5:
        return False
    
    if "@" not in email:
        return False
    
    if "." not in email.split("@")[1]:
        return False
    
    return True
```

### Пример 3: Документация класса

```python
class BankAccount:
    """
    Класс для управления банковским счётом.
    
    Attributes:
        account_number (str): Номер счёта
        balance (float): Текущий баланс
        owner (str): Владелец счёта
    
    Examples:
        >>> account = BankAccount("123456", "Иван Иванов")
        >>> account.deposit(1000)
        >>> account.get_balance()
        1000.0
    """
    
    def __init__(self, account_number, owner):
        """
        Инициализирует новый банковский счёт.
        
        Args:
            account_number (str): Уникальный номер счёта
            owner (str): ФИО владельца счёта
        """
        self.account_number = account_number
        self.owner = owner
        self.balance = 0.0
    
    def deposit(self, amount):
        """
        Вносит деньги на счёт.
        
        Args:
            amount (float): Сумма для внесения
        
        Raises:
            ValueError: Если сумма отрицательная или ноль
        """
        if amount <= 0:
            raise ValueError("Сумма должна быть положительной")
        self.balance += amount
    
    def withdraw(self, amount):
        """
        Снимает деньги со счёта.
        
        Args:
            amount (float): Сумма для снятия
        
        Returns:
            bool: True если операция успешна, False если недостаточно средств
        
        Raises:
            ValueError: Если сумма отрицательная или ноль
        """
        if amount <= 0:
            raise ValueError("Сумма должна быть положительной")
        
        if amount > self.balance:
            return False
        
        self.balance -= amount
        return True
    
    def get_balance(self):
        """
        Возвращает текущий баланс.
        
        Returns:
            float: Текущий баланс счёта
        """
        return self.balance
```

### Пример 4: Документация модуля

```python
"""
Модуль для работы с геометрическими фигурами.

Этот модуль предоставляет функции для вычисления
площадей и периметров различных фигур.

Доступные функции:
    - circle_area(radius)
    - circle_perimeter(radius)
    - rectangle_area(length, width)
    - rectangle_perimeter(length, width)

Автор: Иван Иванов
Дата: 2025-11-25
Версия: 1.0.0
"""

import math

PI = math.pi  # Константа числа Пи


def circle_area(radius):
    """Вычисляет площадь круга."""
    return PI * radius ** 2


def circle_perimeter(radius):
    """Вычисляет длину окружности."""
    return 2 * PI * radius
```

---

## ✅ Задачи для практики

### Задача 1: Документирование функции
Создайте функцию `calculate_bmi(weight, height)` для расчёта индекса массы тела. Добавьте полную документацию.

<details>
<summary>💡 Решение</summary>

```python
def calculate_bmi(weight, height):
    """
    Вычисляет индекс массы тела (BMI).
    
    BMI = вес(кг) / (рост(м))^2
    
    Args:
        weight (float): Вес в килограммах
        height (float): Рост в метрах
    
    Returns:
        float: Индекс массы тела
    
    Raises:
        ValueError: Если вес или рост отрицательные или ноль
    
    Examples:
        >>> calculate_bmi(70, 1.75)
        22.86
    """
    if weight <= 0 or height <= 0:
        raise ValueError("Вес и рост должны быть положительными")
    
    return weight / (height ** 2)
```
</details>

### Задача 2: TODO комментарии
Создайте программу-калькулятор с TODO комментариями для будущих улучшений:
- Добавить логирование операций
- Поддержка истории вычислений
- Работа с комплексными числами

### Задача 3: Рефакторинг комментариев
Дан код с плохими комментариями. Улучшите их:

```python
# увеличиваем i на 1
i = i + 1

# функция сложения
def add(a, b):
    return a + b

# цикл по списку
for item in items:
    # печатаем item
    print(item)
```

---

## 🎓 Стандарты документации

### Google Style

```python
def function(arg1, arg2):
    """
    Summary line.
    
    Extended description.
    
    Args:
        arg1 (int): Description of arg1
        arg2 (str): Description of arg2
    
    Returns:
        bool: Description of return value
    
    Raises:
        ValueError: Description of when this is raised
    """
    pass
```

### NumPy/SciPy Style

```python
def function(arg1, arg2):
    """
    Summary line.
    
    Extended description.
    
    Parameters
    ----------
    arg1 : int
        Description of arg1
    arg2 : str
        Description of arg2
    
    Returns
    -------
    bool
        Description of return value
    
    Raises
    ------
    ValueError
        Description of when this is raised
    """
    pass
```

### Sphinx Style (reStructuredText)

```python
def function(arg1, arg2):
    """
    Summary line.
    
    Extended description.
    
    :param arg1: Description of arg1
    :type arg1: int
    :param arg2: Description of arg2
    :type arg2: str
    :return: Description of return value
    :rtype: bool
    :raises ValueError: Description of when this is raised
    """
    pass
```

---

## 💡 Лучшие практики

### ✅ Что делать

1. **Объясняйте "почему", а не "что"**
   ```python
   # ✅ ХОРОШО
   # Используем UTC для избежания проблем с часовыми поясами
   timestamp = datetime.now(timezone.utc)
   ```

2. **Документируйте публичный API**
   ```python
   # ✅ Все публичные функции должны иметь docstring
   def public_function():
       """Описание функции."""
       pass
   ```

3. **Используйте TODO для временных решений**
   ```python
   # TODO: Заменить на асинхронный запрос
   # FIXME: Не работает для чисел > 1000
   # HACK: Временное решение, требует рефакторинга
   # NOTE: Важная информация для понимания кода
   ```

4. **Обновляйте комментарии вместе с кодом**

### ❌ Чего избегать

1. **Избыточных комментариев**
   ```python
   # ❌ ПЛОХО
   i = 0  # устанавливаем i в 0
   i += 1  # увеличиваем i на 1
   ```

2. **Закомментированного кода**
   ```python
   # ❌ ПЛОХО
   # old_function()
   # another_old_function()
   new_function()
   ```

3. **Бесполезных комментариев**
   ```python
   # ❌ ПЛОХО
   # Это функция
   def calculate():
       pass
   ```

---

## 🔧 Инструменты

### Генерация документации

```bash
# Sphinx
pip install sphinx
sphinx-quickstart
sphinx-build -b html sourcedir builddir

# pydoc (встроенный)
python -m pydoc -w module_name
python -m pydoc -p 8000  # веб-сервер с документацией
```

### Проверка документации

```python
# Проверка примеров в docstring
python -m doctest module.py

# Или в коде
if __name__ == "__main__":
    import doctest
    doctest.testmod()
```

### Просмотр документации

```python
# В интерактивном режиме
help(function_name)
print(function_name.__doc__)

# Информация об объекте
import inspect
print(inspect.signature(function_name))
```

---

## 🔗 Связанные темы

- [Функции](../04_Функции/определение_функций.md)
- [ООП - Классы и объекты](../../Блок_2_ООП_и_продвинутые_концепции/01_ООП/классы_и_объекты.md)
- [Качество кода - PEP 8](../../Блок_5_Git_и_инструменты/03_Качество_кода/pep8.md)

---

## 📖 Ресурсы для изучения

- [PEP 257 - Docstring Conventions](https://www.python.org/dev/peps/pep-0257/)
- [Google Python Style Guide](https://google.github.io/styleguide/pyguide.html#38-comments-and-docstrings)
- [Real Python - Documenting Python Code](https://realpython.com/documenting-python-code/)
- [Sphinx Documentation](https://www.sphinx-doc.org/)

---

[← Вернуться к roadmap](README.md)
