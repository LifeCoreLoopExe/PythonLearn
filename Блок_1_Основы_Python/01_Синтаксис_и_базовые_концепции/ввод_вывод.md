# 🖥️ Ввод и вывод данных

[← Вернуться к roadmap](README.md)

---

## 🎯 Цель изучения

Научиться получать данные от пользователя и выводить информацию на экран в различных форматах.

---

## 📚 Теория

### Функция print()

Основная функция для вывода данных в консоль.

#### Базовое использование

```python
print("Hello, World!")
print(42)
print(3.14)
print(True)
```

#### Вывод нескольких значений

```python
name = "Alice"
age = 25

print("Name:", name, "Age:", age)
# Name: Alice Age: 25

# Изменение разделителя
print("Name:", name, "Age:", age, sep=" | ")
# Name: | Alice | Age: | 25

# Изменение окончания строки
print("Hello", end=" ")
print("World")
# Hello World
```

#### Форматирование вывода

**1. Старый стиль (% форматирование)**
```python
name = "Alice"
age = 25
print("Меня зовут %s и мне %d лет" % (name, age))
```

**2. Метод format()**
```python
print("Меня зовут {} и мне {} лет".format(name, age))
print("Меня зовут {0} и мне {1} лет. Да, {0}!".format(name, age))
print("Меня зовут {name} и мне {age} лет".format(name=name, age=age))
```

**3. f-строки (рекомендуется, Python 3.6+)**
```python
print(f"Меня зовут {name} и мне {age} лет")
print(f"В следующем году мне будет {age + 1} лет")

# Форматирование чисел
price = 1234.5678
print(f"Цена: {price:.2f} руб.")  # Цена: 1234.57 руб.

# Выравнивание
print(f"{'Left':<10}|")    # Left      |
print(f"{'Center':^10}|")  #   Center  |
print(f"{'Right':>10}|")   #      Right|
```

### Функция input()

Получение данных от пользователя. **Всегда возвращает строку!**

```python
name = input("Введите ваше имя: ")
print(f"Привет, {name}!")

# Преобразование типов
age = int(input("Введите ваш возраст: "))
height = float(input("Введите ваш рост (в метрах): "))

print(f"Вам {age} лет, ваш рост {height} м")
```

---

## 💻 Практические примеры

### Пример 1: Анкета пользователя

```python
print("=== АНКЕТА ===\n")

name = input("Как вас зовут? ")
age = int(input("Сколько вам лет? "))
city = input("Из какого вы города? ")
hobby = input("Какое у вас хобби? ")

print("\n" + "="*40)
print("ВАШИ ДАННЫЕ:")
print("="*40)
print(f"Имя:    {name}")
print(f"Возраст: {age} лет")
print(f"Город:   {city}")
print(f"Хобби:   {hobby}")
print("="*40)
```

### Пример 2: Красивый вывод таблицы

```python
products = [
    {"name": "Яблоки", "price": 120.50, "quantity": 5},
    {"name": "Бананы", "price": 89.90, "quantity": 3},
    {"name": "Апельсины", "price": 150.00, "quantity": 4},
]

print("\n" + "="*60)
print(f"{'Товар':<20} {'Цена':>10} {'Количество':>12} {'Сумма':>12}")
print("="*60)

total = 0
for item in products:
    subtotal = item['price'] * item['quantity']
    total += subtotal
    print(f"{item['name']:<20} {item['price']:>10.2f} {item['quantity']:>12} {subtotal:>12.2f}")

print("="*60)
print(f"{'ИТОГО:':<20} {' ':>10} {' ':>12} {total:>12.2f}")
print("="*60 + "\n")
```

### Пример 3: Калькулятор с красивым выводом

```python
print("╔════════════════════════════╗")
print("║      КАЛЬКУЛЯТОР           ║")
print("╚════════════════════════════╝\n")

try:
    num1 = float(input("Введите первое число: "))
    num2 = float(input("Введите второе число: "))
    
    print("\n┌────────────────────────────┐")
    print(f"│ {num1:^10} + {num2:^10} = {num1 + num2:^10.2f}│")
    print(f"│ {num1:^10} - {num2:^10} = {num1 - num2:^10.2f}│")
    print(f"│ {num1:^10} × {num2:^10} = {num1 * num2:^10.2f}│")
    
    if num2 != 0:
        print(f"│ {num1:^10} ÷ {num2:^10} = {num1 / num2:^10.2f}│")
    else:
        print(f"│ {num1:^10} ÷ {num2:^10} =   ERROR!   │")
    
    print("└────────────────────────────┘")

except ValueError:
    print("\n❌ Ошибка: введите корректное число!")
```

### Пример 4: Прогресс бар

```python
import time

def progress_bar(current, total, bar_length=40):
    percent = current / total
    filled = int(bar_length * percent)
    bar = '█' * filled + '░' * (bar_length - filled)
    print(f'\rПрогресс: |{bar}| {percent:.1%} ({current}/{total})', end='')

print("Загрузка файла...")
for i in range(1, 101):
    progress_bar(i, 100)
    time.sleep(0.05)
print("\n✓ Готово!")
```

---

## ✅ Задачи для практики

### Задача 1: Персональная визитка
Создайте программу, которая:
1. Запрашивает: имя, должность, компанию, email, телефон
2. Выводит красивую визитку в рамке

<details>
<summary>💡 Пример вывода</summary>

```
╔════════════════════════════╗
║      Иван Иванов           ║
║   Senior Python Developer  ║
║                            ║
║   Компания: TechCorp       ║
║   Email: ivan@tech.com     ║
║   Тел: +7 999 123-45-67    ║
╚════════════════════════════╝
```
</details>

### Задача 2: Конвертер валют
Создайте программу конвертации валют:
- Запросите сумму в рублях
- Выведите эквивалент в долларах, евро, юанях
- Оформите результат в виде таблицы

### Задача 3: Генератор паролей (вывод)
Создайте несколько случайных паролей и выведите их в виде нумерованного списка с описанием (длина, использованные символы).

### Задача 4: Чек покупки
Создайте программу, которая:
1. Запрашивает товары и цены (минимум 3)
2. Выводит красивый чек с:
   - Названиями товаров
   - Ценами
   - Промежуточной суммой
   - НДС (20%)
   - Итоговой суммой

---

## 🎓 Продвинутые возможности

### 1. Множественный ввод

```python
# Ввод нескольких значений через пробел
data = input("Введите имя и возраст через пробел: ").split()
name, age = data[0], int(data[1])

# Или короче:
name, age = input("Введите имя и возраст: ").split()
age = int(age)
```

### 2. Специальные символы

```python
print("Строка 1\nСтрока 2")      # Перенос строки
print("Колонка1\tКолонка2")       # Табуляция
print("Он сказал: \"Привет!\"")   # Экранирование кавычек
print(r"C:\Users\name\file.txt")  # Raw string (r перед строкой)
print("Строка 1\rПерезапись")     # Возврат каретки
```

### 3. Цветной вывод (ANSI коды)

```python
# Цвета текста
RED = '\033[91m'
GREEN = '\033[92m'
YELLOW = '\033[93m'
BLUE = '\033[94m'
RESET = '\033[0m'

print(f"{RED}Ошибка!{RESET}")
print(f"{GREEN}Успешно!{RESET}")
print(f"{YELLOW}Предупреждение{RESET}")
print(f"{BLUE}Информация{RESET}")
```

### 4. Форматирование чисел

```python
number = 1234567.89

print(f"{number:,}")           # 1,234,567.89
print(f"{number:.2f}")         # 1234567.89
print(f"{number:.2e}")         # 1.23e+06
print(f"{number:015.2f}")      # 0001234567.89

# Проценты
percent = 0.1234
print(f"{percent:.1%}")        # 12.3%

# Бинарный, восьмеричный, шестнадцатеричный
num = 42
print(f"{num:b}")              # 101010
print(f"{num:o}")              # 52
print(f"{num:x}")              # 2a
print(f"{num:X}")              # 2A
```

---

## 💡 Полезные советы

### 1. Валидация ввода

```python
while True:
    try:
        age = int(input("Введите возраст: "))
        if age < 0 or age > 150:
            print("Некорректный возраст!")
            continue
        break
    except ValueError:
        print("Пожалуйста, введите число!")
```

### 2. Значения по умолчанию

```python
name = input("Введите имя (Enter для 'Аноним'): ") or "Аноним"
print(f"Привет, {name}!")
```

### 3. Скрытие ввода пароля

```python
import getpass

password = getpass.getpass("Введите пароль: ")
print("Пароль сохранён (не отображается)")
```

---

## 🔗 Связанные темы

- [Переменные и типы данных](переменные_и_типы_данных.md)
- [Строки и методы обработки](../03_Структуры_данных/строки.md)
- [Обработка исключений](../../Блок_2_ООП_и_продвинутые_концепции/03_Обработка_исключений/try_except.md)

---

## 📖 Ресурсы для изучения

- [Python Docs - Input and Output](https://docs.python.org/3/tutorial/inputoutput.html)
- [Real Python - String Formatting](https://realpython.com/python-f-strings/)
- [PyFormat - Python String Formatting](https://pyformat.info/)

---

**Следующая тема:** [Комментарии и документация →](комментарии.md)

[← Вернуться к roadmap](README.md)
