# ⚙️ Операторы в Python

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)

---

## 🎯 Цель изучения

Освоить работу с арифметическими, логическими операторами и операторами сравнения для выполнения различных вычислений и проверок условий.

---

## 📚 Теория

### 1. Арифметические операторы

```python
a = 10
b = 3

print(a + b)   # 13 - сложение
print(a - b)   # 7  - вычитание
print(a * b)   # 30 - умножение
print(a / b)   # 3.333... - деление (float)
print(a // b)  # 3  - целочисленное деление
print(a % b)   # 1  - остаток от деления
print(a ** b)  # 1000 - возведение в степень
```

### 2. Операторы сравнения

```python
x = 5
y = 10

print(x == y)  # False - равно
print(x != y)  # True  - не равно
print(x > y)   # False - больше
print(x < y)   # True  - меньше
print(x >= y)  # False - больше или равно
print(x <= y)  # True  - меньше или равно
```

### 3. Логические операторы

```python
a = True
b = False

print(a and b)  # False - И (оба должны быть True)
print(a or b)   # True  - ИЛИ (хотя бы один True)
print(not a)    # False - НЕ (инверсия)

# Примеры с числами
x = 5
print(x > 3 and x < 10)  # True
print(x < 3 or x > 10)   # False
print(not (x > 3))       # False
```

### 4. Операторы присваивания

```python
x = 5       # Простое присваивание
x += 3      # x = x + 3  (теперь x = 8)
x -= 2      # x = x - 2  (теперь x = 6)
x *= 4      # x = x * 4  (теперь x = 24)
x /= 3      # x = x / 3  (теперь x = 8.0)
x //= 2     # x = x // 2 (теперь x = 4.0)
x %= 3      # x = x % 3  (теперь x = 1.0)
x **= 2     # x = x ** 2 (теперь x = 1.0)
```

### 5. Операторы принадлежности

```python
text = "Hello, World!"

print("Hello" in text)      # True
print("Python" in text)     # False
print("Python" not in text) # True

numbers = [1, 2, 3, 4, 5]
print(3 in numbers)         # True
print(10 not in numbers)    # True
```

### 6. Операторы тождественности

```python
a = [1, 2, 3]
b = [1, 2, 3]
c = a

print(a == b)   # True  - значения равны
print(a is b)   # False - разные объекты в памяти
print(a is c)   # True  - один и тот же объект
print(a is not b) # True
```

---

## 💻 Практические примеры

### Пример 1: Калькулятор

```python
print("=== Простой калькулятор ===")

num1 = float(input("Введите первое число: "))
operator = input("Введите оператор (+, -, *, /): ")
num2 = float(input("Введите второе число: "))

if operator == "+":
    result = num1 + num2
elif operator == "-":
    result = num1 - num2
elif operator == "*":
    result = num1 * num2
elif operator == "/":
    if num2 != 0:
        result = num1 / num2
    else:
        result = "Ошибка: деление на ноль!"
else:
    result = "Неизвестный оператор"

print(f"Результат: {result}")
```

### Пример 2: Проверка чётности и диапазона

```python
number = int(input("Введите число: "))

# Проверка чётности
is_even = number % 2 == 0
print(f"Число {number} {'чётное' if is_even else 'нечётное'}")

# Проверка диапазона
in_range = 10 <= number <= 100
print(f"Число {'входит' if in_range else 'не входит'} в диапазон [10, 100]")

# Сложное условие
if number > 0 and is_even:
    print("Число положительное и чётное")
elif number > 0 and not is_even:
    print("Число положительное и нечётное")
else:
    print("Число отрицательное или ноль")
```

### Пример 3: Работа со строками

```python
email = input("Введите email: ")

# Проверки
has_at = "@" in email
has_dot = "." in email
is_long = len(email) >= 5
is_valid = has_at and has_dot and is_long

print(f"\nРезультаты проверки:")
print(f"Содержит @: {has_at}")
print(f"Содержит .: {has_dot}")
print(f"Длина >= 5: {is_long}")
print(f"Email {'валидный' if is_valid else 'невалидный'}")
```

### Пример 4: Битовые операторы (дополнительно)

```python
a = 5   # 0101 в двоичной системе
b = 3   # 0011 в двоичной системе

print(f"a & b = {a & b}")   # 1 (0001) - побитовое И
print(f"a | b = {a | b}")   # 7 (0111) - побитовое ИЛИ
print(f"a ^ b = {a ^ b}")   # 6 (0110) - побитовое исключающее ИЛИ
print(f"~a = {~a}")         # -6 - побитовое НЕ
print(f"a << 1 = {a << 1}") # 10 (1010) - сдвиг влево
print(f"a >> 1 = {a >> 1}") # 2 (0010) - сдвиг вправо
```

---

## ✅ Задачи для практики

### Задача 1: Високосный год
Напишите программу, которая проверяет, является ли год високосным.

**Правила:**
- Год високосный, если он делится на 4
- НО если он делится на 100, то он не високосный
- НО если он делится на 400, то он високосный

<details>
<summary>💡 Подсказка</summary>

```python
year = int(input("Введите год: "))
is_leap = (year % 4 == 0 and year % 100 != 0) or (year % 400 == 0)
print(f"Год {year} {'високосный' if is_leap else 'не високосный'}")
```
</details>

### Задача 2: Проверка пароля
Создайте программу, которая проверяет пароль по критериям:
- Длина не менее 8 символов
- Содержит хотя бы одну цифру
- Содержит хотя бы одну заглавную букву

### Задача 3: Конвертер единиц
Напишите программу для конвертации величин:
- Километры ↔ Мили
- Килограммы ↔ Фунты
- Цельсий ↔ Фаренгейт

Программа должна спрашивать направление конвертации.

### Задача 4: Определение четверти
Дано две координаты точки (x, y). Определите, в какой четверти координатной плоскости находится точка, или на какой оси она расположена.

---

## 🎓 Приоритет операторов

От высшего к низшему:

1. `**` - возведение в степень
2. `~`, `+`, `-` - унарные операторы
3. `*`, `/`, `//`, `%` - умножение, деление
4. `+`, `-` - сложение, вычитание
5. `<<`, `>>` - битовые сдвиги
6. `&` - побитовое И
7. `^` - побитовое исключающее ИЛИ
8. `|` - побитовое ИЛИ
9. `==`, `!=`, `>`, `>=`, `<`, `<=`, `is`, `is not`, `in`, `not in` - сравнения
10. `not` - логическое НЕ
11. `and` - логическое И
12. `or` - логическое ИЛИ

```python
# Примеры с приоритетом
print(2 + 3 * 4)      # 14 (не 20)
print((2 + 3) * 4)    # 20 (скобки изменяют порядок)
print(10 > 5 and 3 < 7)  # True
```

---

## 💡 Полезные советы

### 1. Цепочки сравнений
```python
x = 5
# Вместо: x >= 1 and x <= 10
# Можно: 1 <= x <= 10
print(1 <= x <= 10)  # True
```

### 2. Короткое вычисление (short-circuit)
```python
# and останавливается на первом False
result = False and expensive_function()  # expensive_function() не вызовется

# or останавливается на первом True
result = True or expensive_function()  # expensive_function() не вызовется
```

### 3. Использование операторов с разными типами
```python
# Строки
print("Hello" + " " + "World")  # Hello World
print("Ha" * 3)                 # HaHaHa

# Списки
print([1, 2] + [3, 4])  # [1, 2, 3, 4]
print([0] * 5)          # [0, 0, 0, 0, 0]
```

---

## 🔗 Связанные темы

- [Переменные и типы данных](переменные_и_типы_данных.md)
- [Условные операторы](../02_Управляющие_конструкции/условные_операторы.md)
- [Циклы](../02_Управляющие_конструкции/циклы.md)

---

## 📖 Ресурсы для изучения

- [Python Docs - Operators](https://docs.python.org/3/library/operator.html)
- [Real Python - Operators and Expressions](https://realpython.com/python-operators-expressions/)
- [W3Schools - Python Operators](https://www.w3schools.com/python/python_operators.asp)

---

**Следующая тема:** [Ввод и вывод данных →](ввод_вывод.md)

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)
