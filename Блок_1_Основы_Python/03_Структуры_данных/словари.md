# 📖 Словари (Dictionaries)

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)

---

## 🎯 Цель изучения

Освоить работу со словарями - структурой данных для хранения пар ключ-значение.

---

## 📚 Теория

### Создание словарей

```python
# Пустой словарь
empty_dict = {}

# Словарь с данными
person = {
    "name": "Алиса",
    "age": 25,
    "city": "Москва"
}

# Через dict()
person = dict(name="Алиса", age=25, city="Москва")

# Из списка кортежей
items = [("a", 1), ("b", 2), ("c", 3)]
my_dict = dict(items)
```

### Доступ к элементам

```python
person = {"name": "Алиса", "age": 25, "city": "Москва"}

# Получение значения
name = person["name"]  # "Алиса"

# Безопасное получение (с default)
email = person.get("email", "не указан")  # "не указан"

# Проверка наличия ключа
if "age" in person:
    print(f"Возраст: {person['age']}")
```

### Изменение словарей

```python
person = {"name": "Алиса", "age": 25}

# Добавление/изменение
person["email"] = "alice@example.com"
person["age"] = 26

# Обновление из другого словаря
person.update({"city": "Москва", "phone": "+7..."})

# Удаление
del person["email"]
age = person.pop("age")  # Удалить и вернуть значение
person.clear()  # Очистить словарь
```

### Методы словарей

```python
person = {"name": "Алиса", "age": 25, "city": "Москва"}

# Ключи, значения, пары
keys = person.keys()      # dict_keys(['name', 'age', 'city'])
values = person.values()  # dict_values(['Алиса', 25, 'Москва'])
items = person.items()    # dict_items([('name', 'Алиса'), ...])

# Итерация
for key in person:
    print(f"{key}: {person[key]}")

for key, value in person.items():
    print(f"{key}: {value}")
```

---

## 💻 Практические примеры

### Пример 1: Телефонная книга

```python
phonebook = {}

while True:
    print("\n=== ТЕЛЕФОННАЯ КНИГА ===")
    print("1. Добавить контакт")
    print("2. Найти номер")
    print("3. Показать все")
    print("4. Удалить контакт")
    print("5. Выход")
    
    choice = input("Выбор: ")
    
    if choice == "1":
        name = input("Имя: ")
        phone = input("Номер: ")
        phonebook[name] = phone
        print("✅ Добавлено!")
    
    elif choice == "2":
        name = input("Имя: ")
        phone = phonebook.get(name, "Не найдено")
        print(f"📱 {name}: {phone}")
    
    elif choice == "3":
        if phonebook:
            for name, phone in phonebook.items():
                print(f"📱 {name}: {phone}")
        else:
            print("📭 Книга пуста")
    
    elif choice == "4":
        name = input("Имя: ")
        if name in phonebook:
            del phonebook[name]
            print("🗑️ Удалено!")
        else:
            print("❌ Не найдено")
    
    elif choice == "5":
        break
```

### Пример 2: Подсчёт частоты слов

```python
text = "python это язык программирования python очень популярен python"

# Разбиваем текст на слова
words = text.lower().split()

# Подсчитываем частоту
frequency = {}
for word in words:
    frequency[word] = frequency.get(word, 0) + 1

# Выводим результат
for word, count in sorted(frequency.items(), key=lambda x: x[1], reverse=True):
    print(f"{word}: {count}")
```

### Пример 3: Студенты и оценки

```python
students = {
    "Алиса": [85, 90, 88],
    "Боб": [78, 82, 80],
    "Чарли": [92, 95, 93]
}

print("=== УСПЕВАЕМОСТЬ ===")
for student, grades in students.items():
    average = sum(grades) / len(grades)
    print(f"{student}: {average:.1f}")
```

---

## ✅ Задачи для практики

### Задача 1: Инвертирование словаря
Создайте функцию, которая меняет местами ключи и значения в словаре.

### Задача 2: Слияние словарей
Напишите функцию для объединения двух словарей. При совпадении ключей суммируйте значения.

### Задача 3: Анаграммы
Сгруппируйте слова-анаграммы из списка слов.

---

## 📖 Ресурсы для изучения

- [Python Docs - Dictionaries](https://docs.python.org/3/tutorial/datastructures.html#dictionaries)

---

**Следующая тема:** [Множества →](множества.md)

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)
