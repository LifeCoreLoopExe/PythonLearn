# 🔒 Кортежи (Tuples)

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)

---

## 🎯 Цель изучения

Освоить работу с кортежами - неизменяемыми последовательностями в Python.

---

## 📚 Теория

### Что такое кортеж?

Кортеж - это неизменяемая (immutable) последовательность элементов. После создания кортежа его нельзя изменить.

```python
# Создание кортежа
coordinates = (10, 20)
colors = ("red", "green", "blue")
mixed = (1, "text", 3.14, True)

# Кортеж из одного элемента
single = (42,)  # Запятая обязательна!
not_tuple = (42)  # Это просто число в скобках

# Без скобок (tuple packing)
point = 10, 20, 30

# Создание через tuple()
numbers = tuple([1, 2, 3])
letters = tuple("Python")
```

### Доступ к элементам

```python
colors = ("red", "green", "blue", "yellow")

# По индексу
print(colors[0])   # red
print(colors[-1])  # yellow

# Срезы
print(colors[1:3])  # ('green', 'blue')
print(colors[:2])   # ('red', 'green')
```

### Методы кортежей

```python
numbers = (1, 2, 3, 2, 4, 2, 5)

# Подсчёт
count = numbers.count(2)  # 3

# Поиск индекса
index = numbers.index(3)  # 2
```

### Распаковка кортежей

```python
# Простая распаковка
point = (10, 20, 30)
x, y, z = point
print(f"x={x}, y={y}, z={z}")

# Обмен значениями
a, b = 5, 10
a, b = b, a  # Теперь a=10, b=5

# Распаковка с *
first, *middle, last = (1, 2, 3, 4, 5)
print(first)   # 1
print(middle)  # [2, 3, 4]
print(last)    # 5
```

---

## 💻 Практические примеры

### Пример 1: Координаты точек

```python
# Кортежи идеальны для координат
point1 = (10, 20)
point2 = (30, 40)

# Распаковка
x1, y1 = point1
x2, y2 = point2

# Вычисление расстояния
distance = ((x2 - x1)**2 + (y2 - y1)**2)**0.5
print(f"Расстояние: {distance:.2f}")
```

### Пример 2: Возврат нескольких значений из функции

```python
def get_user_info():
    name = "Алиса"
    age = 25
    city = "Москва"
    return name, age, city  # Возвращает кортеж

# Распаковка результата
name, age, city = get_user_info()
print(f"{name}, {age} лет, {city}")
```

### Пример 3: Неизменяемые данные

```python
# Размеры экранов (неизменяемые константы)
SCREEN_SIZES = (
    (1920, 1080),
    (2560, 1440),
    (3840, 2160),
)

for width, height in SCREEN_SIZES:
    print(f"{width}x{height}")
```

---

## 🔄 Кортежи vs Списки

| Критерий | Кортеж | Список |
|----------|--------|--------|
| Изменяемость | ❌ Нет | ✅ Да |
| Скорость | ⚡ Быстрее | 🐢 Медленнее |
| Память | 💾 Меньше | 💾 Больше |
| Использование | Неизменяемые данные | Изменяемые данные |

```python
# Когда использовать кортежи
coordinates = (10, 20)  # ✅ Координаты не меняются
rgb_color = (255, 128, 0)  # ✅ RGB значения константны
date = (2025, 11, 25)  # ✅ Дата неизменна

# Когда использовать списки
tasks = ["задача1", "задача2"]  # ✅ Список задач изменяется
scores = [85, 90, 78]  # ✅ Оценки могут добавляться
```

---

## 📖 Ресурсы для изучения

- [Python Docs - Tuples](https://docs.python.org/3/tutorial/datastructures.html#tuples-and-sequences)

---

**Следующая тема:** [Словари →](словари.md)

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)
