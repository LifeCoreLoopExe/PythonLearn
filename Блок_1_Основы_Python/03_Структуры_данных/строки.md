# ✨ Строки и методы обработки

[← Вернуться к roadmap](README.md)

---

## 🎯 Цель изучения

Освоить работу со строками и методами их обработки.

---

## 📚 Теория

### Создание строк

```python
# Одинарные или двойные кавычки
text1 = 'Hello'
text2 = "World"

# Многострочные строки
multiline = """
Это
многострочная
строка
"""

# F-строки (форматирование)
name = "Алиса"
age = 25
message = f"Меня зовут {name}, мне {age} лет"
```

### Основные методы

```python
text = "  Hello, World!  "

# Регистр
print(text.upper())      # "  HELLO, WORLD!  "
print(text.lower())      # "  hello, world!  "
print(text.capitalize()) # "  hello, world!  "
print(text.title())      # "  Hello, World!  "

# Очистка
print(text.strip())      # "Hello, World!"
print(text.lstrip())     # "Hello, World!  "
print(text.rstrip())     # "  Hello, World!"

# Поиск
print("World" in text)   # True
print(text.find("World")) # 9
print(text.index("World")) # 9 (ошибка если нет)
print(text.count("l"))   # 3

# Замена
print(text.replace("World", "Python"))

# Разбиение
words = "Hello World Python".split()  # ['Hello', 'World', 'Python']
parts = "a,b,c".split(",")  # ['a', 'b', 'c']

# Объединение
joined = " ".join(['Hello', 'World'])  # "Hello World"
```

---

## 💻 Практические примеры

### Пример 1: Валидация email

```python
email = input("Email: ")

if "@" in email and "." in email.split("@")[-1]:
    print("✅ Email корректный")
else:
    print("❌ Email некорректный")
```

### Пример 2: Подсчёт слов

```python
text = input("Введите текст: ")

words = text.split()
print(f"Количество слов: {len(words)}")
print(f"Количество символов: {len(text)}")
print(f"Количество символов без пробелов: {len(text.replace(' ', ''))}")
```

---

## ✅ Задачи для практики

### Задача 1: Проверка палиндрома
Проверьте, является ли строка палиндромом (читается одинаково слева направо и справа налево).

### Задача 2: Цензура
Замените все «плохие слова» в тексте на звёздочки.

---

## 📖 Ресурсы для изучения

- [Python Docs - String Methods](https://docs.python.org/3/library/stdtypes.html#string-methods)

---

[← Вернуться к roadmap](README.md)
