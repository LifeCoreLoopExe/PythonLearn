# 📋 Списки (Lists)

[← Вернуться к roadmap](README.md)

---

## 🎯 Цель изучения

Освоить работу со списками - одной из самых важных структур данных в Python.

---

## 📚 Теория

### Создание списков

```python
# Пустой список
empty_list = []

# Список чисел
numbers = [1, 2, 3, 4, 5]

# Список строк
fruits = ["яблоко", "банан", "апельсин"]

# Список с разными типами
mixed = [1, "текст", 3.14, True, [1, 2, 3]]

# Создание через list()
letters = list("Python")  # ['P', 'y', 't', 'h', 'o', 'n']

# Создание через range
numbers = list(range(5))  # [0, 1, 2, 3, 4]
```

### Доступ к элементам

```python
fruits = ["яблоко", "банан", "апельсин", "киви", "манго"]

# По индексу
print(fruits[0])   # яблоко (первый)
print(fruits[-1])  # манго (последний)
print(fruits[-2])  # киви (предпоследний)

# Срезы (slicing)
print(fruits[1:3])    # ['банан', 'апельсин']
print(fruits[:2])     # ['яблоко', 'банан']
print(fruits[2:])     # ['апельсин', 'киви', 'манго']
print(fruits[::2])    # ['яблоко', 'апельсин', 'манго'] (каждый 2-й)
print(fruits[::-1])   # Реверс списка
```

### Изменение списков

```python
fruits = ["яблоко", "банан", "апельсин"]

# Изменение элемента
fruits[1] = "груша"

# Добавление элемента
fruits.append("киви")          # В конец
fruits.insert(1, "манго")      # На позицию 1

# Удаление элемента
fruits.remove("яблоко")        # По значению
del fruits[0]                  # По индексу
last = fruits.pop()            # Удалить и вернуть последний
specific = fruits.pop(1)       # Удалить и вернуть по индексу

# Очистка списка
fruits.clear()
```

### Методы списков

```python
numbers = [3, 1, 4, 1, 5, 9, 2, 6]

# Сортировка
numbers.sort()                 # На месте
sorted_numbers = sorted(numbers)  # Новый список

# Реверс
numbers.reverse()

# Подсчёт
count = numbers.count(1)       # Сколько раз встречается 1

# Поиск индекса
index = numbers.index(5)       # Индекс первого вхождения

# Расширение
numbers.extend([7, 8, 9])      # Добавить элементы другого списка
numbers += [10, 11]            # Альтернатива
```

---

## 💻 Практические примеры

### Пример 1: Список задач (TODO List)

```python
tasks = []

while True:
    print("\n=== TODO СПИСОК ===")
    print("1. Добавить задачу")
    print("2. Показать задачи")
    print("3. Удалить задачу")
    print("4. Выход")
    
    choice = input("Выбор: ")
    
    if choice == "1":
        task = input("Задача: ")
        tasks.append(task)
        print("✅ Добавлено!")
    
    elif choice == "2":
        if tasks:
            for i, task in enumerate(tasks, 1):
                print(f"{i}. {task}")
        else:
            print("📭 Задач нет")
    
    elif choice == "3":
        if tasks:
            for i, task in enumerate(tasks, 1):
                print(f"{i}. {task}")
            idx = int(input("Номер: ")) - 1
            if 0 <= idx < len(tasks):
                removed = tasks.pop(idx)
                print(f"🗑️ Удалено: {removed}")
        else:
            print("📭 Задач нет")
    
    elif choice == "4":
        break
```

### Пример 2: Анализ списка чисел

```python
numbers = [10, 25, 30, 15, 40, 22, 35]

print(f"Список: {numbers}")
print(f"Длина: {len(numbers)}")
print(f"Сумма: {sum(numbers)}")
print(f"Среднее: {sum(numbers) / len(numbers):.2f}")
print(f"Минимум: {min(numbers)}")
print(f"Максимум: {max(numbers)}")
print(f"Отсортированный: {sorted(numbers)}")
```

---

## ✅ Задачи для практики

### Задача 1: Удаление дубликатов
Создайте функцию, которая удаляет дубликаты из списка, сохраняя порядок элементов.

### Задача 2: Поиск второго максимального
Найдите второе по величине число в списке.

### Задача 3: Объединение списков
Объедините два отсортированных списка в один отсортированный.

---

## 📖 Ресурсы для изучения

- [Python Docs - Lists](https://docs.python.org/3/tutorial/datastructures.html#more-on-lists)
- [Real Python - Python Lists](https://realpython.com/python-lists-tuples/)

---

**Следующая тема:** [Кортежи →](кортежи.md)

[← Вернуться к roadmap](README.md)
