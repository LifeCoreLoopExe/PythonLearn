# 🎭 Множества (Sets)

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)

---

## 🎯 Цель изучения

Освоить работу с множествами - неупорядоченной коллекцией уникальных элементов.

---

## 📚 Теория

### Создание множеств

```python
# Пустое множество (НЕ {})
empty_set = set()

# Множество с элементами
numbers = {1, 2, 3, 4, 5}
fruits = {"яблоко", "банан", "апельсин"}

# Из списка (автоматическое удаление дубликатов)
numbers = set([1, 2, 2, 3, 3, 4])  # {1, 2, 3, 4}
```

### Операции с множествами

```python
a = {1, 2, 3, 4}
b = {3, 4, 5, 6}

# Объединение (union)
print(a | b)  # {1, 2, 3, 4, 5, 6}
print(a.union(b))

# Пересечение (intersection)
print(a & b)  # {3, 4}
print(a.intersection(b))

# Разность (difference)
print(a - b)  # {1, 2}
print(a.difference(b))

# Симметричная разность
print(a ^ b)  # {1, 2, 5, 6}
print(a.symmetric_difference(b))
```

### Методы множеств

```python
fruits = {"яблоко", "банан"}

# Добавление
fruits.add("апельсин")

# Удаление
fruits.remove("банан")  # Ошибка если нет
fruits.discard("киви")  # Без ошибки

# Очистка
fruits.clear()
```

---

## 💻 Практические примеры

### Пример 1: Удаление дубликатов

```python
numbers = [1, 2, 2, 3, 4, 4, 5]
unique = list(set(numbers))
print(unique)  # [1, 2, 3, 4, 5]
```

### Пример 2: Проверка уникальности email

```python
emails = [
    "user1@example.com",
    "user2@example.com",
    "user1@example.com",  # Дубликат
    "user3@example.com"
]

unique_emails = set(emails)
if len(emails) != len(unique_emails):
    print("⚠️ Обнаружены дубликаты!")
    duplicates = len(emails) - len(unique_emails)
    print(f"Количество дубликатов: {duplicates}")
```

---

## 📖 Ресурсы для изучения

- [Python Docs - Sets](https://docs.python.org/3/tutorial/datastructures.html#sets)

---

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)
