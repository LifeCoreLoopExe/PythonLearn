# 🌐 Область видимости переменных

[← Вернуться к roadmap](README.md)

---

## 🎯 Цель изучения

Понять области видимости переменных: локальную, глобальную и нелокальную.

---

## 📚 Теория

### Локальная область видимости

```python
def my_function():
    local_var = 10  # Локальная переменная
    print(local_var)

my_function()  # 10
# print(local_var)  # Ошибка! Переменная не существует вне функции
```

### Глобальная область видимости

```python
global_var = 100  # Глобальная переменная

def my_function():
    print(global_var)  # Можем читать

my_function()  # 100
print(global_var)  # 100
```

### Изменение глобальной переменной

```python
counter = 0

def increment():
    global counter  # Объявляем, что используем глобальную
    counter += 1

increment()
print(counter)  # 1
```

### Нелокальная область (nonlocal)

```python
def outer():
    x = 10
    
    def inner():
        nonlocal x  # Ссылка на переменную из внешней функции
        x += 5
        print(f"Inner: {x}")
    
    inner()
    print(f"Outer: {x}")

outer()
# Inner: 15
# Outer: 15
```

---

## 💻 Практические примеры

### Пример 1: Счётчик вызовов

```python
call_count = 0

def track_calls():
    global call_count
    call_count += 1
    print(f"Функция вызвана {call_count} раз")

track_calls()  # 1
track_calls()  # 2
track_calls()  # 3
```

---

## 💡 Лучшие практики

- ✅ Избегайте излишнего использования глобальных переменных
- ✅ Передавайте данные через параметры функций
- ✅ Возвращайте результаты из функций

```python
# ❌ Плохо (глобальная переменная)
total = 0

def add_to_total(value):
    global total
    total += value

# ✅ Хорошо (параметры и возврат)
def add(current_total, value):
    return current_total + value

total = add(total, 5)
```

---

[← Вернуться к roadmap](README.md)
