# 🔧 Определение и вызов функций

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)

---

## 🎯 Цель изучения

Научиться создавать и использовать функции для организации кода.

---

## 📚 Теория

### Базовый синтаксис

```python
def function_name():
    """Docstring - описание функции"""
    # Тело функции
    print("Привет из функции!")

# Вызов функции
function_name()
```

### Параметры и аргументы

```python
# Позиционные параметры
def greet(name, age):
    print(f"Привет, {name}! Тебе {age} лет.")

greet("Алиса", 25)

# Параметры по умолчанию
def greet(name, age=18):
    print(f"Привет, {name}! Тебе {age} лет.")

greet("Боб")  # age = 18 по умолчанию
greet("Чарли", 30)

# Именованные аргументы
def create_user(name, age, city):
    print(f"{name}, {age}, {city}")

create_user(name="Алиса", city="Москва", age=25)
```

### Возвращаемые значения

```python
def add(a, b):
    return a + b

result = add(5, 3)
print(result)  # 8

# Возврат нескольких значений
def get_min_max(numbers):
    return min(numbers), max(numbers)

minimum, maximum = get_min_max([1, 5, 3, 9, 2])
```

### *args и **kwargs

```python
# *args - произвольное количество позиционных аргументов
def sum_all(*numbers):
    return sum(numbers)

print(sum_all(1, 2, 3, 4, 5))  # 15

# **kwargs - произвольное количество именованных аргументов
def print_info(**info):
    for key, value in info.items():
        print(f"{key}: {value}")

print_info(name="Алиса", age=25, city="Москва")
```

---

## 💻 Практические примеры

### Пример 1: Калькулятор

```python
def calculator(a, b, operation):
    """
    Простой калькулятор.
    
    Args:
        a (float): Первое число
        b (float): Второе число
        operation (str): Операция (+, -, *, /)
    
    Returns:
        float: Результат операции
    """
    if operation == "+":
        return a + b
    elif operation == "-":
        return a - b
    elif operation == "*":
        return a * b
    elif operation == "/":
        return a / b if b != 0 else "Ошибка: деление на ноль"
    else:
        return "Неизвестная операция"

result = calculator(10, 5, "+")
print(result)  # 15
```

### Пример 2: Проверка пароля

```python
def is_password_strong(password):
    """
    Проверяет надёжность пароля.
    
    Критерии:
    - Длина >= 8 символов
    - Есть цифры
    - Есть заглавные буквы
    - Есть строчные буквы
    """
    if len(password) < 8:
        return False, "Пароль слишком короткий"
    
    has_digit = any(char.isdigit() for char in password)
    has_upper = any(char.isupper() for char in password)
    has_lower = any(char.islower() for char in password)
    
    if not has_digit:
        return False, "Нет цифр"
    if not has_upper:
        return False, "Нет заглавных букв"
    if not has_lower:
        return False, "Нет строчных букв"
    
    return True, "Пароль надёжный"

is_strong, message = is_password_strong("Abc123")
print(f"{message}")
```

---

## ✅ Задачи для практики

### Задача 1: Факториал
Напишите функцию для вычисления факториала числа.

### Задача 2: Числа Фибоначчи
Создайте функцию, возвращающую N-ое число Фибоначчи.

### Задача 3: Палиндром
Функция проверки, является ли строка палиндромом.

---

## 📖 Ресурсы для изучения

- [Python Docs - Functions](https://docs.python.org/3/tutorial/controlflow.html#defining-functions)
- [Real Python - Defining Functions](https://realpython.com/defining-your-own-python-function/)

---

**Следующая тема:** [Область видимости →](область_видимости.md)

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)
