# 📦 Важные модули стандартной библиотеки

[← Вернуться к roadmap](README.md)

---

## 🎯 Цель изучения

Освоить работу с важными модулями стандартной библиотеки Python.

---

## 📚 Основные модули

### 1. os - работа с операционной системой

```python
import os

# Текущая директория
print(os.getcwd())

# Список файлов в директории
print(os.listdir('.'))

# Создание директории
os.makedirs('new_folder', exist_ok=True)

# Проверка существования
print(os.path.exists('file.txt'))

# Работа с путями
path = os.path.join('folder', 'subfolder', 'file.txt')
print(os.path.basename(path))  # file.txt
print(os.path.dirname(path))   # folder/subfolder
```

### 2. sys - системные параметры

```python
import sys

# Версия Python
print(sys.version)

# Аргументы командной строки
print(sys.argv)

# Путь поиска модулей
print(sys.path)

# Выход из программы
# sys.exit(0)
```

### 3. datetime - работа с датой и временем

```python
from datetime import datetime, timedelta

# Текущая дата и время
now = datetime.now()
print(now)

# Форматирование
print(now.strftime("%Y-%m-%d %H:%M:%S"))

# Арифметика с датами
tomorrow = now + timedelta(days=1)
week_ago = now - timedelta(weeks=1)

# Парсинг строки в дату
date = datetime.strptime("2025-11-25", "%Y-%m-%d")
```

### 4. math - математические функции

```python
import math

print(math.pi)          # 3.14159...
print(math.sqrt(16))    # 4.0
print(math.ceil(4.3))   # 5
print(math.floor(4.8))  # 4
print(math.pow(2, 3))   # 8.0
```

### 5. random - генерация случайных чисел

```python
import random

# Случайное число от 0 до 1
print(random.random())

# Случайное целое число
print(random.randint(1, 10))

# Выбор случайного элемента
fruits = ['яблоко', 'банан', 'апельсин']
print(random.choice(fruits))

# Перемешивание списка
random.shuffle(fruits)
```

### 6. re - регулярные выражения

```python
import re

text = "Мой email: user@example.com"

# Поиск
match = re.search(r'\w+@\w+\.\w+', text)
if match:
    print(match.group())  # user@example.com

# Замена
new_text = re.sub(r'\d+', 'X', "Телефон: 123-456")
print(new_text)  # Телефон: XXX-XXX

# Поиск всех совпадений
numbers = re.findall(r'\d+', "10 яблок, 20 груш, 30 слив")
print(numbers)  # ['10', '20', '30']
```

---

## 💻 Практические примеры

### Пример 1: Работа с файловой системой

```python
import os
from datetime import datetime

def list_files_with_info(directory='.'):
    """Выводит список файлов с информацией"""
    for item in os.listdir(directory):
        path = os.path.join(directory, item)
        size = os.path.getsize(path)
        mtime = os.path.getmtime(path)
        modified = datetime.fromtimestamp(mtime)
        
        file_type = "DIR" if os.path.isdir(path) else "FILE"
        print(f"{file_type:5} {size:10} bytes {modified:%Y-%m-%d %H:%M} {item}")

list_files_with_info()
```

### Пример 2: Генератор паролей

```python
import random
import string

def generate_password(length=12):
    """Генерирует случайный пароль"""
    characters = string.ascii_letters + string.digits + string.punctuation
    password = ''.join(random.choice(characters) for _ in range(length))
    return password

print("Ваш пароль:", generate_password())
```

---

## 📖 Ресурсы для изучения

- [Python Docs - Standard Library](https://docs.python.org/3/library/)

---

[← Вернуться к roadmap](README.md)
