# 📚 Git - Основы системы контроля версий

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)

---

## 🎯 Цель изучения

Освоить основные команды Git для работы с системой контроля версий.

---

## 📚 Основные команды

### Инициализация репозитория

```bash
# Создать новый репозиторий
git init

# Клонировать существующий репозиторий
git clone https://github.com/user/repo.git
```

### Базовый workflow

```bash
# 1. Проверить статус
git status

# 2. Добавить файлы в индекс
git add file.py              # Конкретный файл
git add .                    # Все файлы
git add *.py                 # Все .py файлы

# 3. Зафиксировать изменения
git commit -m "Описание изменений"

# 4. Отправить на сервер
git push origin main
```

### Работа с ветками

```bash
# Создать новую ветку
git branch feature-name

# Переключиться на ветку
git checkout feature-name

# Создать и переключиться (одной командой)
git checkout -b feature-name

# Список веток
git branch

# Удалить ветку
git branch -d feature-name

# Слияние веток
git checkout main
git merge feature-name
```

### Получение изменений

```bash
# Получить изменения с сервера
git pull origin main

# Скачать без слияния
git fetch origin
```

### Просмотр истории

```bash
# История коммитов
git log

# Краткая история
git log --oneline

# Графическое представление
git log --graph --oneline --all
```

### Отмена изменений

```bash
# Отменить изменения в файле
git checkout -- file.py

# Убрать файл из индекса
git reset HEAD file.py

# Отменить последний коммит (сохранить изменения)
git reset --soft HEAD~1

# Отменить последний коммит (удалить изменения)
git reset --hard HEAD~1
```

---

## 💻 Практический пример workflow

```bash
# 1. Клонировать проект
git clone https://github.com/user/project.git
cd project

# 2. Создать новую ветку для фичи
git checkout -b add-login-feature

# 3. Внести изменения в код
# (редактируем файлы)

# 4. Проверить изменения
git status
git diff

# 5. Добавить и закоммитить
git add .
git commit -m "Добавлена форма входа"

# 6. Отправить ветку на сервер
git push origin add-login-feature

# 7. Создать Pull Request на GitHub

# 8. После одобрения - слить с main
git checkout main
git pull origin main
git merge add-login-feature

# 9. Удалить ветку
git branch -d add-login-feature
```

---

## 🔧 Конфигурация

```bash
# Настроить имя и email
git config --global user.name "Ваше Имя"
git config --global user.email "your@email.com"

# Посмотреть конфигурацию
git config --list

# Установить редактор по умолчанию
git config --global core.editor "code --wait"
```

---

## 📝 .gitignore

Файл `.gitignore` указывает, какие файлы не отслеживать:

```gitignore
# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Другое
*.log
.env
secrets.txt
```

---

## 💡 Лучшие практики

### ✅ Рекомендуется

```bash
# Частые коммиты с понятными сообщениями
git commit -m "Добавлен метод валидации email"

# Работа в отдельных ветках
git checkout -b feature/user-auth

# Регулярное обновление из main
git checkout main
git pull
git checkout feature/user-auth
git merge main
```

### Структура commit message

```
<тип>: <краткое описание>

<подробное описание (опционально)>

<футер (опционально)>
```

Типы:
- `feat` - новая функциональность
- `fix` - исправление бага
- `docs` - документация
- `style` - форматирование
- `refactor` - рефакторинг
- `test` - тесты
- `chore` - рутинные задачи

Пример:
```
feat: добавлена функция логина

Реализована форма входа с валидацией email и пароля.
Добавлены тесты для новой функциональности.

Closes #123
```

---

## 📖 Ресурсы для изучения

- [Git Documentation](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Learn Git Branching](https://learngitbranching.js.org/) - интерактивный туториал

---

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)
