# 📁 Чтение и запись файлов

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)

---

## 🎯 Цель изучения

Научиться работать с файлами: читать данные из файлов и записывать данные в файлы.

---

## 📚 Теория

### Открытие файлов

```python
# Режимы открытия файлов:
# 'r' - чтение (read) - по умолчанию
# 'w' - запись (write) - перезаписывает файл
# 'a' - добавление (append) - добавляет в конец
# 'x' - создание (exclusive) - ошибка если файл существует
# 'b' - бинарный режим (например, 'rb', 'wb')
# '+' - чтение и запись (например, 'r+', 'w+')
```

### Чтение файлов

```python
# Способ 1: Простое открытие
file = open('file.txt', 'r', encoding='utf-8')
content = file.read()
print(content)
file.close()  # Важно закрыть файл!

# Способ 2: Контекстный менеджер (рекомендуется)
with open('file.txt', 'r', encoding='utf-8') as file:
    content = file.read()
    print(content)
# Файл автоматически закроется

# Чтение построчно
with open('file.txt', 'r', encoding='utf-8') as file:
    for line in file:
        print(line.strip())  # strip() убирает \n

# Чтение всех строк в список
with open('file.txt', 'r', encoding='utf-8') as file:
    lines = file.readlines()
```

### Запись в файлы

```python
# Запись (перезапись файла)
with open('output.txt', 'w', encoding='utf-8') as file:
    file.write("Привет, мир!\n")
    file.write("Вторая строка\n")

# Добавление в конец файла
with open('output.txt', 'a', encoding='utf-8') as file:
    file.write("Дополнительная строка\n")

# Запись списка строк
lines = ["Строка 1\n", "Строка 2\n", "Строка 3\n"]
with open('output.txt', 'w', encoding='utf-8') as file:
    file.writelines(lines)
```

---

## 💻 Практические примеры

### Пример 1: Счётчик слов в файле

```python
def count_words(filename):
    """Подсчитывает количество слов в файле"""
    try:
        with open(filename, 'r', encoding='utf-8') as file:
            content = file.read()
            words = content.split()
            return len(words)
    except FileNotFoundError:
        print(f"❌ Файл {filename} не найден")
        return 0

# Использование
word_count = count_words('text.txt')
print(f"Количество слов: {word_count}")
```

### Пример 2: Журнал (Log)

```python
from datetime import datetime

def write_log(message):
    """Записывает сообщение в лог-файл"""
    timestamp = datetime.now().strftime("%Y-%m-%d %H:%M:%S")
    log_entry = f"[{timestamp}] {message}\n"
    
    with open('app.log', 'a', encoding='utf-8') as file:
        file.write(log_entry)

# Использование
write_log("Приложение запущено")
write_log("Пользователь вошёл в систему")
write_log("Ошибка подключения к БД")
```

### Пример 3: Копирование файла

```python
def copy_file(source, destination):
    """Копирует файл"""
    try:
        with open(source, 'r', encoding='utf-8') as src:
            content = src.read()
        
        with open(destination, 'w', encoding='utf-8') as dst:
            dst.write(content)
        
        print(f"✅ Файл скопирован: {source} → {destination}")
    except FileNotFoundError:
        print(f"❌ Исходный файл {source} не найден")
    except Exception as e:
        print(f"❌ Ошибка: {e}")

# Использование
copy_file('original.txt', 'copy.txt')
```

### Пример 4: TODO список в файле

```python
class TodoList:
    """Класс для управления списком задач в файле"""
    
    def __init__(self, filename='todos.txt'):
        self.filename = filename
    
    def add_task(self, task):
        """Добавить задачу"""
        with open(self.filename, 'a', encoding='utf-8') as file:
            file.write(f"{task}\n")
        print(f"✅ Задача добавлена: {task}")
    
    def show_tasks(self):
        """Показать все задачи"""
        try:
            with open(self.filename, 'r', encoding='utf-8') as file:
                tasks = file.readlines()
                if tasks:
                    print("\n📝 Список задач:")
                    for i, task in enumerate(tasks, 1):
                        print(f"{i}. {task.strip()}")
                else:
                    print("📭 Список задач пуст")
        except FileNotFoundError:
            print("📭 Файл задач ещё не создан")
    
    def clear_tasks(self):
        """Очистить все задачи"""
        with open(self.filename, 'w', encoding='utf-8') as file:
            pass  # Просто открываем в режиме 'w', файл очистится
        print("🗑️ Все задачи удалены")

# Использование
todo = TodoList()
todo.add_task("Изучить Python")
todo.add_task("Написать код")
todo.add_task("Протестировать программу")
todo.show_tasks()
```

---

## ✅ Задачи для практики

### Задача 1: Анализ текста
Создайте программу, которая читает текстовый файл и выводит статистику:
- Количество строк
- Количество слов
- Количество символов
- Самое длинное слово

### Задача 2: Замена в файле
Напишите программу, которая заменяет все вхождения одного слова на другое в текстовом файле.

### Задача 3: Объединение файлов
Создайте программу, которая объединяет несколько текстовых файлов в один.

---

## 💡 Лучшие практики

### ✅ Рекомендуется

```python
# 1. Всегда используйте контекстный менеджер
with open('file.txt', 'r') as file:  # ✅
    content = file.read()

# 2. Указывайте кодировку
with open('file.txt', 'r', encoding='utf-8') as file:  # ✅
    content = file.read()

# 3. Обрабатывайте исключения
try:
    with open('file.txt', 'r') as file:
        content = file.read()
except FileNotFoundError:
    print("Файл не найден")
```

### ❌ Не рекомендуется

```python
# 1. Открытие без закрытия
file = open('file.txt', 'r')  # ❌
content = file.read()
# Файл не закрыт!

# 2. Без указания кодировки
file = open('file.txt', 'r')  # ❌ Может вызвать проблемы
```

---

## 📖 Ресурсы для изучения

- [Python Docs - Reading and Writing Files](https://docs.python.org/3/tutorial/inputoutput.html#reading-and-writing-files)
- [Real Python - Working With Files](https://realpython.com/working-with-files-in-python/)

---

**Следующая тема:** [Работа с CSV →](работа_с_csv.md)

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)
