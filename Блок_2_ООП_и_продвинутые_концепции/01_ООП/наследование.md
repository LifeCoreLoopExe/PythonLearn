# 👨‍👦 Наследование

[← Вернуться к roadmap](README.md)

---

## 🎯 Цель изучения

Освоить механизм наследования для повторного использования кода и создания иерархий классов.

---

## 📚 Теория

### Базовое наследование

```python
# Родительский (базовый) класс
class Animal:
    def __init__(self, name):
        self.name = name
    
    def speak(self):
        pass

# Дочерний (производный) класс
class Dog(Animal):
    def speak(self):
        return f"{self.name} говорит: Гав!"

class Cat(Animal):
    def speak(self):
        return f"{self.name} говорит: Мяу!"

# Использование
dog = Dog("Бобик")
cat = Cat("Мурка")

print(dog.speak())  # Бобик говорит: Гав!
print(cat.speak())  # Мурка говорит: Мяу!
```

### Вызов методов родительского класса (super())

```python
class Vehicle:
    def __init__(self, brand, model):
        self.brand = brand
        self.model = model
    
    def info(self):
        return f"{self.brand} {self.model}"

class Car(Vehicle):
    def __init__(self, brand, model, doors):
        super().__init__(brand, model)  # Вызов конструктора родителя
        self.doors = doors
    
    def info(self):
        parent_info = super().info()  # Вызов метода родителя
        return f"{parent_info}, Дверей: {self.doors}"

car = Car("Toyota", "Camry", 4)
print(car.info())  # Toyota Camry, Дверей: 4
```

### Множественное наследование

```python
class Flyable:
    def fly(self):
        return "Летает"

class Swimmable:
    def swim(self):
        return "Плавает"

class Duck(Flyable, Swimmable):
    def quack(self):
        return "Кря-кря"

duck = Duck()
print(duck.fly())    # Летает
print(duck.swim())   # Плавает
print(duck.quack())  # Кря-кря
```

---

## 💻 Практические примеры

### Пример 1: Иерархия сотрудников

```python
class Employee:
    """Базовый класс сотрудника"""
    
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary
    
    def get_info(self):
        return f"{self.name}: {self.salary} руб/мес"

class Manager(Employee):
    """Менеджер с бонусом"""
    
    def __init__(self, name, salary, bonus):
        super().__init__(name, salary)
        self.bonus = bonus
    
    def get_info(self):
        base_info = super().get_info()
        total = self.salary + self.bonus
        return f"{base_info} + бонус {self.bonus} = {total} руб"

class Developer(Employee):
    """Разработчик с языком программирования"""
    
    def __init__(self, name, salary, language):
        super().__init__(name, salary)
        self.language = language
    
    def get_info(self):
        base_info = super().get_info()
        return f"{base_info}, Язык: {self.language}"

# Использование
manager = Manager("Иван", 80000, 20000)
developer = Developer("Алиса", 100000, "Python")

print(manager.get_info())
print(developer.get_info())
```

---

## 📖 Ресурсы для изучения

- [Python Docs - Inheritance](https://docs.python.org/3/tutorial/classes.html#inheritance)
- [Real Python - Inheritance and Composition](https://realpython.com/inheritance-composition-python/)

---

[← Вернуться к roadmap](README.md)
