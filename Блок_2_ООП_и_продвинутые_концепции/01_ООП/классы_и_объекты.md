# 🏛️ Классы и объекты

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)

---

## 🎯 Цель изучения

Освоить основы объектно-ориентированного программирования (ООП) в Python.

---

## 📚 Теория

### Создание класса

```python
class Dog:
    """Класс, представляющий собаку"""
    
    def __init__(self, name, age):
        """Конструктор класса"""
        self.name = name  # Атрибут экземпляра
        self.age = age
    
    def bark(self):
        """Метод класса"""
        print(f"{self.name} говорит: Гав!")
    
    def get_info(self):
        """Возвращает информацию о собаке"""
        return f"{self.name}, {self.age} лет"

# Создание объекта (экземпляра класса)
my_dog = Dog("Бобик", 3)
my_dog.bark()  # Бобик говорит: Гав!
print(my_dog.get_info())  # Бобик, 3 лет
```

### Атрибуты класса vs атрибуты экземпляра

```python
class Dog:
    # Атрибут класса (общий для всех экземпляров)
    species = "Canis familiaris"
    
    def __init__(self, name, age):
        # Атрибуты экземпляра (уникальны для каждого)
        self.name = name
        self.age = age

dog1 = Dog("Бобик", 3)
dog2 = Dog("Шарик", 5)

print(dog1.species)  # Canis familiaris
print(dog2.species)  # Canis familiaris
print(dog1.name)     # Бобик
print(dog2.name)     # Шарик
```

---

## 💻 Практические примеры

### Пример 1: Банковский счёт

```python
class BankAccount:
    """Класс банковского счёта"""
    
    def __init__(self, owner, balance=0):
        self.owner = owner
        self.balance = balance
    
    def deposit(self, amount):
        """Пополнение счёта"""
        if amount > 0:
            self.balance += amount
            print(f"✅ Пополнено на {amount} руб. Баланс: {self.balance} руб.")
        else:
            print("❌ Сумма должна быть положительной")
    
    def withdraw(self, amount):
        """Снятие средств"""
        if amount > self.balance:
            print("❌ Недостаточно средств")
        elif amount > 0:
            self.balance -= amount
            print(f"✅ Снято {amount} руб. Баланс: {self.balance} руб.")
        else:
            print("❌ Сумма должна быть положительной")
    
    def get_balance(self):
        """Получение баланса"""
        return f"Баланс: {self.balance} руб."

# Использование
account = BankAccount("Иван", 1000)
print(account.get_balance())
account.deposit(500)
account.withdraw(300)
print(account.get_balance())
```

### Пример 2: Студент

```python
class Student:
    """Класс студента"""
    
    def __init__(self, name, student_id):
        self.name = name
        self.student_id = student_id
        self.grades = []
    
    def add_grade(self, grade):
        """Добавить оценку"""
        if 0 <= grade <= 100:
            self.grades.append(grade)
            print(f"✅ Оценка {grade} добавлена")
        else:
            print("❌ Оценка должна быть от 0 до 100")
    
    def get_average(self):
        """Получить средний балл"""
        if self.grades:
            return sum(self.grades) / len(self.grades)
        return 0
    
    def __str__(self):
        """Строковое представление объекта"""
        avg = self.get_average()
        return f"Студент: {self.name} (ID: {self.student_id}), Средний балл: {avg:.2f}"

# Использование
student = Student("Алиса", "S001")
student.add_grade(85)
student.add_grade(92)
student.add_grade(78)
print(student)
```

---

## ✅ Задачи для практики

### Задача 1: Класс Rectangle
Создайте класс прямоугольника с методами вычисления площади и периметра.

### Задача 2: Класс Book
Создайте класс книги с информацией о названии, авторе, годе издания и методом для красивого вывода.

### Задача 3: Класс ShoppingCart
Создайте класс корзины покупок с возможностью добавления/удаления товаров и подсчёта общей стоимости.

---

## 📖 Ресурсы для изучения

- [Python Docs - Classes](https://docs.python.org/3/tutorial/classes.html)
- [Real Python - OOP in Python](https://realpython.com/python3-object-oriented-programming/)

---

**Следующая тема:** [Наследование →](наследование.md)

[← Вернуться к roadmap](../../Python_Junior_Roadmap.md)
